DATABASE_URL="postgresql://postgres:[password]@[host]:5432/postgres?schema=public"
DIRECT_URL="postgresql://postgres:[password]@[host]:5432/postgres?schema=public"
# WEB_ORIGIN="https://your-pages-domain.pages.dev"  # Optional: restrict CORS to the Cloudflare Pages domain in production. Defaults to * (safe — no cookies/credentials used).
